services:
  api:
    build: .
    command: >
      sh -c "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8080"
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8080:8080"